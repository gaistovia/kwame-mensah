# /assets/icons

Reserved for custom icon/favicon assets.

The demo currently uses a small inline SVG data-URI as its favicon and
inline SVG markup for the expertise-card icons in `index.html`. Drop a
proper favicon set (`favicon.ico`, `apple-touch-icon.png`, etc.) and any
exported icon files here if this project moves toward production.
